# Page-view routing

Each sidebar item opens one isolated view within the bordered application shell. Inactive views are hidden explicitly on desktop and mobile, and the active navigation item is marked for accessibility.
